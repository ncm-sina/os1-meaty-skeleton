#include <stdio.h>
#include <stdlib.h>

int main(void) {
    // printf("MyOS Shell\n");
    while (1) {
        // printf("%s ", getenv("prompt"));
        // TODO: Read and execute commands
    }
    return 0;
}
